# Redacted Codex protocol fixtures

Fixtures in this directory must come from the local app-server and be recursively redacted before commit. They must not contain credentials, account identifiers, email addresses, thread content, user directories, or project absolute paths.
